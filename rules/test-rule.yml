id: test-hardcoded-secret
message: "Detected hardcoded secret"
severity: warning
language: javascript
rule:
  pattern: |
    const secret = "$SECRET"
  where:
    SECRET:
      regex: "[a-zA-Z0-9]{20,}"
